# ============================================================
# requirements.txt — Dashboard Marine Sème | METEO-BENIN
# Déploiement Streamlit Cloud
# ============================================================

# ── Dashboard ────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.22.0
pandas>=2.2.0
numpy>=1.26.0
openpyxl>=3.1.2
pytz>=2024.1
python-docx>=1.1.0

# ── Export PNG des graphiques (optionnel) ────────────────────
# Décommenter si kaleido est disponible sur Streamlit Cloud
# kaleido>=0.2.1

# ── Pipeline ECMWF / Copernicus (requis pour mode "live") ────
streamlit>=1.30.0
pandas>=2.0.0
ecmwf-opendata>=0.3.0
requests>=2.31.0
numpy>=1.24.0
plotly>=5.18.0
copernicusmarine>=1.3.0
cfgrib>=0.9.12
eccodes>=2.35.0
xarray>=2024.1.0
requests>=2.31.0

# ── Exports Word (optionnel) ─────────────────────────────────
# python-docx>=1.1.0

# ── Upload Google Drive (optionnel) ──────────────────────────
# google-api-python-client>=2.130.0
# google-auth-httplib2>=0.2.0
# google-auth-oauthlib>=1.2.0
